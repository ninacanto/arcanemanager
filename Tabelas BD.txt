-- Criação da tabela Senhas
CREATE TABLE Senhas (
    ID NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY,
    DESCRICAO VARCHAR2(40) NOT NULL,
    CATEGORIA VARCHAR2(50),
    LOGIN VARCHAR2(40) NOT NULL,
    SENHA VARCHAR2(40) NOT NULL,
    PRIMARY KEY (ID)
);